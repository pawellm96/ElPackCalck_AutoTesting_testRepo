# Как запустить тесты и посмотреть результаты

## 🚀 Способ 1: Запуск тестов в GitHub Actions (автоматически)

### Автоматический запуск
Тесты автоматически запускаются при каждом:
- Push в ветки `main`, `master` или `develop`
- Создании Pull Request

### Ручной запуск через GitHub
1. Откройте ваш репозиторий: https://github.com/pawellm96/ElPackCalck_AutoTesting_testRepo
2. Перейдите во вкладку **"Actions"**
3. В левом меню выберите workflow **"CI"**
4. Нажмите кнопку **"Run workflow"** (справа вверху)
5. Выберите ветку и нажмите **"Run workflow"**

### Просмотр результатов в GitHub Actions
1. Откройте вкладку **"Actions"** в репозитории
2. Выберите нужный запуск (run) из списка
3. В деталях запуска вы увидите:
   - ✅ **Зеленая галочка** - все тесты прошли успешно
   - ❌ **Красный крестик** - есть ошибки
   - ⚠️ **Желтый кружок** - предупреждения
4. Нажмите на job **"build-and-test"** для деталей
5. Разверните секцию **"Run tests"** чтобы увидеть вывод тестов
6. Разверните секцию **"Publish test results"** для детального отчета

### Скачать артефакты с результатами
1. В деталях запуска найдите секцию **"Artifacts"** (внизу страницы)
2. Нажмите на **"test-results"**
3. Скачайте архив с результатами тестов

---

## 💻 Способ 2: Запуск тестов локально

### Клонирование репозитория
```bash
# Клонировать репозиторий
git clone https://github.com/pawellm96/ElPackCalck_AutoTesting_testRepo.git
cd ElPackCalck_AutoTesting_testRepo
```

### Запуск тестов через .NET CLI
```bash
# Восстановить зависимости
dotnet restore

# Собрать решение
dotnet build

# Запустить все тесты
dotnet test

# Запустить тесты с подробным выводом
dotnet test --verbosity detailed

# Запустить тесты конкретного проекта
dotnet test ElPackCalc.Tests/ElPackCalc.Tests.csproj

# Запустить тесты с фильтром по имени
dotnet test --filter "FullyQualifiedName~ListElectricalCircuitsTests"
```

### Запуск тестов через Visual Studio
1. Откройте `ElPackCalc.sln` в Visual Studio
2. Откройте **Test Explorer** (Test → Test Explorer)
3. Нажмите **"Run All Tests"** или выберите конкретные тесты

### Просмотр результатов локально
После запуска `dotnet test` вы увидите в консоли:
```
Passed!  - Failed:     0, Passed:     5, Skipped:     0, Total:     5
```

Для детального отчета:
```bash
# Создать отчет в формате TRX
dotnet test --logger "trx;LogFileName=test-results.trx"

# Отчет будет в папке TestResults
```

---

## 📊 Форматы результатов тестов

### В GitHub Actions
- **Сводка тестов** отображается прямо в интерфейсе
- **Детальный отчет** доступен в секции "Publish test results"
- **Артефакты** содержат файлы `.trx` и отчеты о покрытии

### Локально
- **Консольный вывод** - сразу после `dotnet test`
- **TRX файлы** - XML отчеты в папке `TestResults/`
- **Coverage отчеты** - если включен сбор покрытия кода

---

## 🔍 Примеры команд

### Быстрый запуск
```bash
dotnet test
```

### С подробным выводом
```bash
dotnet test --verbosity detailed --logger "console;verbosity=detailed"
```

### Только определенные тесты
```bash
# По имени класса
dotnet test --filter "FullyQualifiedName~ListElectricalCircuitsTests"

# По имени метода
dotnet test --filter "FullyQualifiedName~GetPolesNumber"
```

### С отчетом о покрытии
```bash
dotnet test --collect:"XPlat Code Coverage"
```

---

## ❓ Решение проблем

### Тесты не запускаются
- Убедитесь, что установлен .NET SDK 8.0 или выше
- Проверьте, что зависимости восстановлены: `dotnet restore`

### Ошибки компиляции
- Проверьте, что Revit API доступен (для локальной разработки)
- В CI/CD тесты работают без Revit через моки

### Тесты падают
- Проверьте логи в GitHub Actions
- Запустите тесты локально с `--verbosity detailed`
- Проверьте, что все моки настроены правильно

---

## 📝 Полезные ссылки

- [Репозиторий на GitHub](https://github.com/pawellm96/ElPackCalck_AutoTesting_testRepo)
- [GitHub Actions](https://github.com/pawellm96/ElPackCalck_AutoTesting_testRepo/actions)
- [Документация xUnit](https://xunit.net/)
- [Документация Moq](https://github.com/moq/moq4)

